#!/bin/bash
# This script is public domain
if [ -z "$1" ]; then
 echo no params
 /usr/local/bin/links http://herb
else
 echo params
 echo "$1" "$2" "$3" "$4" "$5" "$6" "$7" "$8" "$9"
 /usr/local/bin/links "$*"
fi
